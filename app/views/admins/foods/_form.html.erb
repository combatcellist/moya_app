<%= form_with(model: admins_food, local: true) do |form| %>
  <% if admins_food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admins_food.errors.count, "error") %> prohibited this admins_food from being saved:</h2>

      <ul>
        <% admins_food.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
